{
  "name": "<%= projectName %>",
  "private": true,
  "version": "0.0.1",
  "description": "<%= projectDesc %>",
  "keywords": [],
  "scripts": {
    "postinstall": "bower install",
    "start": "$(npm bin)/gulp serve",
    "test": "mocha"
  }
}
